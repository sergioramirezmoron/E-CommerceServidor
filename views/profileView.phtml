<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil - E-Commerce</title>
    <link rel="stylesheet" href="public/css/main.css">
    <link rel="stylesheet" href="public/css/forms.css">
    <link rel="stylesheet" href="public/css/cart.css">
</head>

<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <nav class="nav">
                <a href="index.php" class="logo">🛒 E-Commerce</a>
                <ul class="nav-links">
                    <?php if (!empty($_SESSION['user'])) { ?>
                        <li><a href="index.php?c=cart&cart=<?php echo CartRepository::getCartByUserId($_SESSION["user"]->getId())->getId(); ?>">🛍️ Mi Carrito</a></li>
                        <li><a href="index.php?c=user&profile=1">👤 Mi Perfil</a></li>
                        <?php if ($_SESSION['user']->getRol()) { ?>
                            <li><a href="index.php?c=product&create=1">➕ Crear Producto</a></li>
                            <li><a href="index.php?c=category&create=1">📁 Crear Categoría</a></li>
                        <?php } ?>
                    <?php } ?>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Contenido Principal -->
    <main class="main-content">
        <div class="container">
            <div class="profile-container">
                <div class="profile-header">
                    <h1 class="profile-title">👤 Mi Perfil</h1>
                    <p class="profile-subtitle">Gestiona tu información personal</p>
                </div>

                <?php if ($_SESSION['user'] && $_SESSION['user'] !== false): ?>
                    <div class="profile-content">
                        <div class="profile-avatar">
                            <?php if ($_SESSION['user']->getAvatar()) { ?>
                                <img src="public/img/<?php echo $_SESSION['user']->getAvatar(); ?>"
                                    alt="Imagen de perfil"
                                    class="avatar-image">
                            <?php } else { ?>
                                <div class="avatar-placeholder">
                                    <span class="avatar-icon">👤</span>
                                </div>
                            <?php } ?>
                        </div>

                        <div class="profile-info">
                            <div class="info-card">
                                <div class="info-item">
                                    <span class="info-label">👤 Nombre de usuario:</span>
                                    <span class="info-value"><?php echo $_SESSION['user']->getUsername(); ?></span>
                                </div>

                                <div class="info-item">
                                    <span class="info-label">📧 Email:</span>
                                    <span class="info-value"><?php echo $_SESSION['user']->getEmail(); ?></span>
                                </div>

                                <div class="info-item">
                                    <span class="info-label">📱 Teléfono:</span>
                                    <span class="info-value"><?php echo $_SESSION['user']->getPhone(); ?></span>
                                </div>

                                <div class="info-item">
                                    <span class="info-label">🔑 Tipo de cuenta:</span>
                                    <span class="info-value account-type">
                                        <?php echo $_SESSION['user']->getRol() ? '👑 Administrador' : '👤 Usuario'; ?>
                                    </span>
                                </div>
                            </div>

                            <div class="profile-actions">
                                <a href="index.php" class="btn btn-primary">🏠 Volver al Inicio</a>
                                <a href="index.php?c=user&logout=1" class="btn btn-danger">Cerrar Sesión</a>
                            </div>
                        </div>
                    </div>
                <?php else: ?>
                    <div class="empty-profile">
                        <div class="empty-profile-icon">👤</div>
                        <h2>No hay información de perfil</h2>
                        <p>Inicia sesión para ver tu perfil</p>
                        <a href="index.php?c=user&login=1" class="btn btn-primary">Iniciar Sesión</a>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 E-Commerce. Todos los derechos reservados.</p>
        </div>
    </footer>

</body>

</html>