<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo $product->getName(); ?> - E-Commerce</title>
    <link rel="stylesheet" href="public/css/main.css">
    <link rel="stylesheet" href="public/css/forms.css">
    <link rel="stylesheet" href="public/css/cart.css">
</head>

<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <nav class="nav">
                <a href="index.php" class="logo">🛒 E-Commerce</a>
                <ul class="nav-links">
                    <?php if (!empty($_SESSION['user'])) { ?>
                        <li><a href="index.php?c=user&logout=1">Cerrar Sesión</a></li>
                        <li><a href="index.php?c=cart&cart=<?php echo CartRepository::getCartByUserId($_SESSION["user"]->getId())->getId(); ?>">🛍️ Mi Carrito</a></li>
                        <li><a href="index.php?c=user&profile=1">👤 Mi Perfil</a></li>
                        <?php if ($_SESSION['user']->getRol()) { ?>
                            <li><a href="index.php?c=product&create=1">➕ Crear Producto</a></li>
                            <li><a href="index.php?c=category&create=1">📁 Crear Categoría</a></li>
                        <?php } ?>
                    <?php } ?>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Contenido Principal -->
    <main class="main-content">
        <div class="container">
            <div class="product-detail">
                <div class="product-detail-content">
                    <div class="product-detail-image">
                        <img src="public/img/<?php echo $product->getImage(); ?>"
                            alt="<?php echo $product->getName(); ?>"
                            class="product-main-image">
                    </div>

                    <div class="product-detail-info">
                        <h1 class="product-detail-title"><?php echo $product->getName(); ?></h1>

                        <div class="product-detail-price">
                            <span class="price-label">Precio:</span>
                            <span class="price-amount">$<?php echo number_format($product->getPrice(), 2); ?></span>
                        </div>

                        <div class="product-detail-stock">
                            <span class="stock-label">Stock disponible:</span>
                            <span class="stock-amount"><?php echo $product->getStock(); ?> unidades</span>
                        </div>

                        <div class="product-detail-description">
                            <h3>Descripción del Producto</h3>
                            <p><?php echo nl2br($product->getDescription()); ?></p>
                        </div>

                        <div class="product-detail-actions">
                            <?php if (!empty($_SESSION['user'])) { ?>
                                <a href="index.php?c=productCart&cart=<?php echo CartRepository::getCartByUserId($_SESSION["user"]->getId())->getId(); ?>&add=<?php echo $product->getId(); ?>"
                                    class="btn btn-success btn-large">🛒 Agregar al Carrito</a>
                            <?php } else { ?>
                                <a href="index.php?c=user&login=1" class="btn btn-primary btn-large">Iniciar Sesión para Comprar</a>
                            <?php } ?>

                            <?php if (!empty($_SESSION['user']) && $_SESSION['user']->getRol()) { ?>
                                <a href="index.php?c=product&edit=1&id=<?php echo $product->getId(); ?>"
                                    class="btn btn-outline">✏️ Editar Producto</a>
                            <?php } ?>

                            <a href="index.php" class="btn btn-outline">← Volver a la Tienda</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 E-Commerce. Todos los derechos reservados.</p>
        </div>
    </footer>

</body>

</html>