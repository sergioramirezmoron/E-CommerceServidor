<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto - E-Commerce</title>
    <link rel="stylesheet" href="public/css/main.css">
    <link rel="stylesheet" href="public/css/forms.css">
</head>

<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <nav class="nav">
                <a href="index.php" class="logo">🛒 E-Commerce</a>
                <ul class="nav-links">
                    <?php if (!empty($_SESSION['user'])) { ?>
                        <li><a href="index.php?c=cart&cart=<?php echo CartRepository::getCartByUserId($_SESSION["user"]->getId())->getId(); ?>">🛍️ Mi Carrito</a></li>
                        <li><a href="index.php?c=user&profile=1">👤 Mi Perfil</a></li>
                        <?php if ($_SESSION['user']->getRol()) { ?>
                            <li><a href="index.php?c=product&create=1">➕ Crear Producto</a></li>
                            <li><a href="index.php?c=category&create=1">📁 Crear Categoría</a></li>
                        <?php } ?>
                    <?php } ?>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Contenido Principal -->
    <main class="main-content">
        <div class="container">
            <div class="product-form">
                <h1 class="form-title">✏️ Editar Producto</h1>
                <p class="text-center mb-4">Modifica la información del producto</p>

                <form action="index.php?c=product&edit=1&id=<?php echo $product->getId(); ?>" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label class="form-label" for="name">Nombre del Producto</label>
                        <input type="text"
                            name="name"
                            id="name"
                            value="<?php echo $product->getName(); ?>"
                            class="form-input"
                            required>
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="description">Descripción</label>
                        <textarea name="description"
                            id="description"
                            class="form-textarea"
                            required><?php echo $product->getDescription(); ?></textarea>
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="price">Precio</label>
                        <input type="number"
                            step="0.01"
                            name="price"
                            id="price"
                            value="<?php echo $product->getPrice(); ?>"
                            class="form-input"
                            required>
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="stock">Stock Disponible</label>
                        <input type="number"
                            step="1"
                            name="stock"
                            id="stock"
                            value="<?php echo $product->getStock(); ?>"
                            class="form-input"
                            required>
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="idCategoria">Categoría</label>
                        <select name="idCategoria" id="idCategoria" class="form-select" required>
                            <option value="1" <?php echo $product->getCategory() == 1 ? 'selected' : ''; ?>>Electrónica</option>
                            <option value="2" <?php echo $product->getCategory() == 2 ? 'selected' : ''; ?>>Comida</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="productImage">Imagen del Producto</label>
                        <div class="file-input-wrapper">
                            <input type="file"
                                name="productImage"
                                id="productImage"
                                class="file-input"
                                accept="image/*">
                            <label for="productImage" class="file-input-label">
                                📷 Cambiar imagen del producto
                            </label>
                        </div>
                        <small class="form-help">Imagen actual: <?php echo $product->getImage(); ?> (deja vacío para mantener la actual)</small>
                    </div>

                    <div class="form-actions">
                        <button type="submit" name="updateProductSubmit" class="btn btn-success btn-large">
                            ✅ Actualizar Producto
                        </button>
                        <a href="index.php?c=product&id=<?php echo $product->getId(); ?>" class="btn-cancel">Cancelar</a>
                        <a href="index.php" class="btn btn-outline">🏠 Volver al Inicio</a>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 E-Commerce. Todos los derechos reservados.</p>
        </div>
    </footer>

</body>

</html>